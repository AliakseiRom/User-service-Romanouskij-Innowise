spring:
  application:
    name: user-service
  datasource:
    url: "jdbc:postgresql://localhost:5432/user_service"
    username: "postgres"
    password: "12345"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"